/**
 * Given a two sorted (ascending) integer arrays A and B.
 * Return array C that is a result of merging A and B (C is also sorted).
 *
 * E.g. A = [1, 2, 3, 6, 7]
 *      B = [2, 5, 12, 13]
 * Result:
 *      C = [1, 2, 2, 3, 5, 6, 7, 12, 13]
 */
public class MergeSortedArrays {
    public int[] merge(int[] a, int[] b) {
    }
}
